# 🎨 Livestart 赛博炫酷风 Vue 3 高并发演示客户端

本客户端是专为 Livestart 微服务抢票平台量身定制的高保真、前后端分离前端系统。融合了 **大麦网（粉红）** 与 **秀动网（暗黑）** 的设计精髓，采用**现代 Vue 3 + 赛博朋克多主题 CSS 变量重绘** 技术构建。

它不仅具备极致的视觉冲击力（WOW 效果），还能**直观、生动地将看不见的后端核心技术亮点（防刷 Token 生成、RocketMQ 异步排队削峰、16表分库分表资金核算）以动画和穿透卡片的形式呈现出来**，是毕业设计答辩与成果展示的绝对加分神器！

---

## 🚀 极速启动与使用指南

我们提供 **“双核自适应驱动”** 设计，极度人性化，拥有以下两种启动方式：

### 方式一：双击即开，100% 免安装依赖（推荐极速演示）
如果您本地没有配置 Node.js/NPM 环境，或者因为网络环境原因导致安装包下载报错：
1. **直接进入 `client/` 目录**。
2. **在浏览器中直接双击打开 `index.html`**。
3. 界面会通过浏览器的现代 ES Modules 映射导入 (Importmap) 技术，自动从高速 CDN 节点拉取 Vue 3，无需进行任何前端工程打包编译！
4. 默认会激活导航栏右侧的 **“离线 Mock 模式”**，您可以在此模式下体验完整的搜索热词排行、下单 Token 防刷动画、RocketMQ 队列排队倒计时、订单支付回调（核销码生成）、取消及退票库存自愈流转、以及跨 16 张分表的商户结算穿透对账，所有交互和动画均完全模拟真实微服务，开箱即用！

### 方式二：Vite + 网关代理本地真实联调
如果您想与后端 Java 微服务群进行真实的数据互通和联调：
1. 确保已启动网关服务（Gateway，端口 `8888`）以及相关的后端微服务。
2. 打开终端，进入 `client` 目录，执行依赖安装及启动：
   ```bash
   cd client
   npm install
   npm run dev
   ```
   说明：`npm run dev` 与 `npm run dev:local` 等价，都会使用 Vite 本地代理转发 `/api/live-start/**`，适合本地联调。
   若需要验证发布配置，再使用 `npm run dev:publish`，该模式会直接请求 `http://localhost:8888`，浏览器可能遇到跨域限制。
3. Vite 开发服务器启动后，会自动打开浏览器（默认端口 `3000`）。
4. 在页面顶部导航栏右侧，**勾选关闭“离线 Mock 模式”**（使滑块流转为**“网关联调模式”**，状态指示灯将变绿）。
5. 此时，所有前端操作（搜索、下单 Token、购票下单、订单管理、票房结算）都将经由 Vite 服务器代理并直通后端的 `http://localhost:8888` 网关，完成 100% 前后端物理闭环！

---

## 🎨 四色主题皮肤一键切换
我们在顶部导航栏右侧配置了**“四色主题一键重绘”**按钮。点击可在以下四套精美设计系统中随时切换，网页的背景、霓虹阴影、高亮卡片及主色调会在微秒级内全部平滑重绘，提供极致视觉享受：
1. **🌌 赛博暗黑风**：深邃蓝黑底 + 霓虹粉红 + 霓虹青绿（极客最爱 Livehouse 风格）。
2. **☀️ 高雅极简风**：极简雅白底 + 商务蔚蓝 + 活力绿（大众官网主流大气风格）。
3. **🔴 大麦炽红风**：碳素黑底 + 炽热大麦红 + 质金黄（致敬大麦网官方炽红抢票版）。
4. **🟢 秀动荧光绿**：硬核石墨灰 + 科技荧光绿 + 荧光紫（秀动潮流电子乐现场风格）。

---

## 🎯 答辩演示舱三大核武亮点

1. **演出检索与热搜云词条**
   - 搜索关键字后，前端会自动向 Redis ZSet 送入词条，并在头部高亮渲染热搜排行，支持点击直接触发搜索。
2. **高并发抢票体验舱**
   - 点击“抢票” ➔ 动态展示防刷网关加载动画（`/api/engine/order/token`） ➔ 高亮获取到的单次有效 pathToken ➔ 携带 Token 访问异步下单 ➔ 动态展示 RocketMQ 排队削峰百分比进度条 ➔ 成功返回订单号 ➔ 流转超时未支付自愈。
3. **分库分表票房穿透看板**
   - 一键触发结算（`/api/settlement/trigger`） ➔ 炫酷逐一扫描物理表 `t_order_item_0` 到 `t_order_item_15` 的动画 ➔ 展示数字翻滚的销售总额、出票量、5%平台佣金与实结净额 ➔ 穿透渲染这 16 张物理订单分表的散列柱状卡片，将分库分表技术直观且具象地秀给评委！
---

## Docker 说明

当前仓库没有提供“整套 Livestart 微服务一键 Docker Compose 启动方案”。`docker/` 目录目前只包含 XXL-JOB Admin 的容器编排：

```text
docker/xxl-job/docker-compose.yml
```

这个 Compose 主要用于给 `services/distribution` 提供调度后台，适合联调以下能力：

- 开售放票任务
- 开售提醒任务
- 依赖 XXL-JOB 的一次性定时任务

启动方式：

```bash
cd docker/xxl-job
docker compose up -d
```

启动后访问：

```text
http://127.0.0.1:8080/xxl-job-admin
```

注意事项：

- Compose 默认连接宿主机 MySQL：`host.docker.internal:3306/xxl_job`
- 当前示例账号密码写在 `docker/xxl-job/docker-compose.yml` 中，默认是 `root / 123456`
- 如果你的本地 MySQL 配置不同，需要先修改 `PARAMS` 里的数据源配置
- 如果你只需要看前端页面、演出列表、下单流程，通常不必启动 XXL-JOB
- 如果你要联调定时放票、提醒任务，请同时启动 `services/distribution`

推荐顺序：

1. 先启动 MySQL、Redis、RocketMQ 等基础依赖
2. 需要定时任务能力时，再启动 `docker/xxl-job`
3. 启动网关和后端微服务
4. 最后在 `client` 目录执行 `npm run dev`
